<!-- 
    Either "merge" this file with the actual build script of the project,
    or
    in case of a simple (no dependencies) project, just use this and:
    MAVEN_OPTS="-Xmx2G" mvn clean package -DgroupId=org.example.foo -DartifactId=bar -Dversion=0.5
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>${groupId}</groupId>
    <artifactId>${artifactId}</artifactId>
    <version>${version}</version>
    <build>
        <defaultGoal>package</defaultGoal>

        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerVersion>1.6</compilerVersion>
                    <proc>none</proc>
                    <optimize>true</optimize>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>metajar</id>
                        <phase>package</phase>
                        <goals><goal>jar</goal></goals>
                        <configuration>
                            <includes>
                                <include>**/*_.class</include>
                            </includes>
                            <outputDirectory>${project.build.directory}/bar</outputDirectory>
                            <finalName>${project.artifactId}-meta-${project.version}</finalName>
                            <archive><addMavenDescriptor>false</addMavenDescriptor></archive>
                        </configuration>
                    </execution>
                    <execution>
                        <id>metasrcjar</id>
                        <phase>package</phase>
                        <goals><goal>jar</goal></goals>
                        <configuration>
                            <classifier>sources</classifier>
                            <classesDirectory>${project.build.directory}/foo</classesDirectory>
                            <includes>
                                <include>**/*.java</include>
                            </includes>
                            <outputDirectory>${project.build.directory}/bar</outputDirectory>
                            <finalName>${project.artifactId}-meta-${project.version}</finalName>
                            <archive><addMavenDescriptor>false</addMavenDescriptor></archive>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.bsc.maven</groupId>
                <artifactId>maven-processor-plugin</artifactId>
                <version>2.2.4</version>
                <executions>
                    <execution>
                        <id>process</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>process</goal>
                        </goals>
                        <configuration>
                            <compilerArguments>-source 1.5 -target 1.5</compilerArguments>
                            <outputDirectory>${project.build.directory}/foo</outputDirectory>
                            <optionMap>
                                <CommonMetadataProcessor.onlyPublicMembers>true</CommonMetadataProcessor.onlyPublicMembers>
                            </optionMap>
                            <processors>
                                <processor>fi.solita.utils.codegen.CommonMetadataProcessor</processor>
                            </processors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>fi.solita.utils</groupId>
            <artifactId>functional-utils</artifactId>
            <version>0.1-SNAPSHOT</version>
        </dependency>
    </dependencies>
</project>